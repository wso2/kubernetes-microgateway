apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "choreo-connect.fullname" . }}-test-connection"
  labels:
    {{- include "choreo-connect.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "choreo-connect.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
