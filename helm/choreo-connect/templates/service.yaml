apiVersion: v1
kind: Service
metadata:
  name: {{ include "choreo-connect.fullname" . }}
  labels:
    {{- include "choreo-connect.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "choreo-connect.selectorLabels" . | nindent 4 }}
